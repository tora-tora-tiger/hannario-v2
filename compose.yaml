services:
  letta:
    image: letta/letta:latest
    container_name: hannario-letta
    env_file:
      - .env.letta
    ports:
      - "8283:8283"
    volumes:
      - letta_pgdata:/var/lib/postgresql/data
      - ./logs:/logs:ro
      - ./data:/data

volumes:
  letta_pgdata:
